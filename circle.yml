dependencies:
  pre:
    - case $CIRCLE_NODE_INDEX in 0) NODE_VERSION=0.12 ;; 1) NODE_VERSION=4 ;; 2) NODE_VERSION=6 ;; 3) NODE_VERSION=6.1 ;; esac; nvm install $NODE_VERSION && nvm alias default $NODE_VERSION
