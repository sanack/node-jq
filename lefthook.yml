pre-commit:
  commands:
    validate-commit-msg:
      run: validate-commit-msg

pre-push:
  parallel: true
  commands:
    test:
      files: git diff --name-only HEAD @{push}
      run: npm run test
    lint:
      files: git diff --name-only HEAD @{push}
      run: npm run lint
